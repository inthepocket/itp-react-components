import * as React from 'react';

/**
 *  TODO: should be replaced by @inthepocket/itp-rcc-input
 */
const Input = (props) => (
  <input {...props} data-testid="autocomplete-input" />
);

export default Input;